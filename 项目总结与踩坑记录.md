# 北林 CS2 Major — 项目日志与维护记录

本文件是项目唯一的持续工作日志，记录重要变更、关键决策、验证结果和待办。日常进度与临时分析只在对话中汇报；不再按任务建立独立总结。历史技术复盘保留在文末。

使用说明见 [README](README.md)，部署操作见 [部署指南](deploy.md)，测试命令和覆盖范围见 [测试说明](backend/tests/README.md)，名人堂规则见 [功能设计](docs/design/名人堂功能设计.md)。

## 当前状态与待办（截至 2026-09-16）

- 此前累积功能已归档并同步远程，招募筛选提交为 `440d51a`。后续变更、后端部署与小程序发布分别记录，历史条目的“未提交”仅代表当时状态。
- 9 月 16 日已部署取消个人 QQ 收集、展示与催填的后端；建队不再强制 QQ，接口不返回历史 QQ，旧列和历史数据保留兼容。新版小程序仍由维护者上传、审核并发布。
- 管理员定向站内通知后端已于 9 月 16 日上线，`admin_notices` 迁移已完成。新版小程序发布后可在管理工作台选择人员发送，收件人主页显示未读提示、消息页确认已读；此次未向真实用户发送通知。
- 9 月 11 日约 14:09 已部署后端版本 `20260911-140444`，包含 BO3 错误处理、认证候选学号、管理待办修复；执行了 `manual_champions` 与 `verification_student_id` 两项迁移。此次未上传或发布小程序。
- 9 月 11 日已在上述线上版本单独部署测试数据隐藏补丁 `visibility-20260911-1555`，执行 `test_data_visibility` 迁移；按维护者确认仅保留赛事 ID 7，隐藏脚本 BOT 账号及队伍。未捎带工作区约赛消息或小程序修改，未提交、推送 Git。
- 9 月 12 日已单独部署学号位数判定补丁 `identity-20260912-005553`，只更新认证、AI 审核说明和报名提示三处后端服务文件，无数据库迁移，无小程序发布。约赛消息功能仍未随本次发布。
- 9 月 12 日已部署约赛消息版本 `schedule-20260912-113916`，执行 `schedule_notifications` 迁移；随后部署招募与通知版本 `recruitment-20260912-120827`。约赛、招募、主页消息与申请资料后端均已上线，小程序新界面待上传审核发布。
- 线上微信订阅已移除旧 `captain_qq_reminder` 配置，保留入队邀请、入队申请共用「队伍通知」模板，字段为 `phrase6=event`、`thing7=team_name`。定向通知与约赛使用站内消息。发布和配置说明见 `deploy.md` 第十一、十二节。
- 待验证：新版小程序的认证、管理表单、消息通知真机交互，以及真实 MySQL 的约赛并发行为。隔离测试与离线预览不代表这些验证已经完成。
- 图片上传的隐私授权前端缺陷已修复，等待体验版真机验证及维护者上传审核发布；不能通过后端部署修复已发布的小程序代码，无需后端或数据库变更。
- 队伍列表与分阶段战绩已在本地前端修正，待上传小程序：半决赛胜者及时显示决赛，阶段战绩从历史对阵独立计算。后端原有 `wins/losses/diff` 接口字段未变更，新列表不再用其排序或显示。

## 变更记录

### 2026-09-19 本地 PyTorch 学习环境

- 学习脚本 `ceshi.py/ceshi.py` 的 `import torch` 失败，原因是项目根目录 `.venv`（Python 3.13.14）缺少 PyTorch。在该环境通过官方 CUDA 12.8 源安装 `torch 2.11.0+cu128`，并补充 `numpy 2.5.3`，用于本地 RTX 5070 Laptop GPU 学习实验。
- 验证：原脚本输出 `2.11.0+cu128`、`True`，GPU 矩阵运算和 NumPy / GPU 张量转换通过，`pip check` 无依赖冲突。VS Code 使用项目根目录 `.venv/Scripts/python.exe` 解释器运行。

### 2026-09-16 定向通知与 QQ 整改后端部署

- 按维护者指令部署已提交版本 `a71e4f1` 的后端，发布编号 `admin-notices-20260916-2125`。核对线上运行镜像、服务器源码与本地差异，仅 13 个源码文件涉及定向通知和 QQ 整改；依赖、compose、nginx 配置一致，沿用现有镜像依赖构建。
- 停止旧后端写入后备份数据库、原配置和源码，执行 `admin_notices` 迁移两次，新增 `admin_notice_batches`、`admin_notices` 两张空表。迁移前后原有 16 张表的行数、全量数据摘要和字段结构一致；历史 QQ 列和值未删除，未创建测试账号或通知。
- 服务器 `.env` 仅移除微信模板中的 `captain_qq_reminder`，保留原邀请、申请模板和其他配置；新后端不再收集或返回个人 QQ。切换镜像并刷新 nginx 后，实际运行的全部 51 个 Python 源文件与发布清单一致，无启动错误。
- 验证：本地与实际待发布镜像各通过 269 项后端回归；线上 19 项只读接口验收通过，涵盖管理员选人、发送记录、用户收件箱、主页计数、未登录 401、普通用户访问管理接口 403，以及游客/登录者/管理员队伍详情隐私。真实 MySQL 的唯一约束、索引和外键核验通过；公网赛事、队伍、招募、微信配置和 OpenAPI 正常。未向真人发送测试消息，不等同于微信真机交互或实际并发写入验收。
- 回滚数据库、原源码、配置、旧镜像标识及验证结果保留于 `/home/ubuntu/releases/admin-notices-20260916-2125`；本次上传包、构建/测试目录和临时配置已清理。未上传发布小程序，未提交或推送本次日志；维护者要求后续 Git 提交、推送说明和用户通知文案使用中文。

### 2026-09-15 新生赛策划案承办单位调整

- 按维护者要求，将 `docs/策划案/北京林业大学CS2新生赛策划案.docx` 中 5 处「动漫社游戏部」统一改为「动漫社、五环社」，同步活动简介、主办及承办单位、组织方式、人员分工和赛后归档表述。
- 验证：文档内部组件对比确认仅指定文字变更，原样保留其他内容及格式；使用 Word 导出并检查全部 7 页，无文字遮挡或表格溢出。标准渲染器因缺少 LibreOffice 未运行成功，改用 Word 完成排版核验；本次临时预览文件用后清理，未提交或推送 Git。

### 2026-09-15 个人 QQ 审核整改与管理员定向站内通知（待部署）

- 微信审核证据指向创建队伍收集队长 QQ。维护者明确选择移除个人 QQ 收集和展示，改用现有申请、邀请与约赛消息；随后要求新增管理员选择具体人员发送站内通知，主页提示参照管理待办。
- 建队仅填写队伍名称，删除队伍、目录和管理详情的 QQ 输入、编辑、展示、复制与催填入口。后端不再校验或保存旧客户端多传的 `captain_qq`，所有队伍响应不返回历史值；旧联系更新入口鉴权后返回 410。历史数据库列与迁移保留兼容，不自动删除真实历史资料；公开的管理员支持联系方式不属于用户资料收集，保持原有用途。
- 微信模板配置过滤停用的催填类型，接受旧共享模板不再生成 QQ 提醒，待发送旧事件不会投递；正常入队申请和邀请保持原订阅、队列与路由。删除本次涉及的废弃联系表单、校验、样式和预览状态，原专项测试更新为防止恢复 QQ 收集的回归。
- 管理工作台「常用管理 → 定向通知」仅管理员可用，审核员不可发送。按昵称、游戏 ID 或用户 ID 搜索，跨页逐个选择最多 100 人，填写最多 40 字标题、500 字正文后预览确认。收件人目录不展示学号、截图、QQ 或微信标识。后台可查看最近 20 批发送人数和已读人数。
- 新增 `admin_notice_batches` 与 `admin_notices`，每位收件人独立已读状态。按固定 ID 顺序锁用户，批次和收件记录同事务提交；收件人失效时整批回滚。同一管理员的同一请求 ID 可安全重试，改换内容复用 ID 返回 409；前端防重复点击、保留失败草稿，账号变化/退出页面后忽略迟到结果。
- 主页的个人消息卡突出显示管理员通知未读数与最新标题，点击定位消息页；用户打开正文并确认后才标记已读。分页不标记已读，读取和更新必须属于当前收件人，历史已读通知仍可查看。此次功能为站内通知，不自动发送微信通知，不需要新微信模板。
- 验证：269 项 Python、276 项 Node 回归通过；22 份 WXML、24 份 WXSS 原生编译通过。375/320 宽虚构资料预览检查选人、标题/正文、主页提示、消息页和无 QQ 建队表单，无横向溢出。自动测试不访问线上数据库或发送真人消息，不替代实际 MySQL 并发、微信真机与平台审核。
- 发布：先部署后端、执行幂等 `python -m app.migrations.admin_notices`，再由维护者上传审核发布包含隐私授权修复的新小程序。步骤已合并到 `deploy.md`，此次未部署线上、未上传小程序、未提交或推送 Git；预览在内存运行并用后关闭，没有新增临时文件入库。

### 2026-09-15 图片上传隐私授权卡住（前端待发布）

- 根因：全局 `onNeedPrivacyAuthorization` 用普通 `showModal` 充当授权，确认时仅传 `event: agree`，缺少微信要求的 `agreePrivacyAuthorization` 按钮及其 `buttonId`；拒绝分支没有结束原接口等待，也没有打开官方隐私指引的入口。此前已经同意隐私指引的账号通常绕过该监听，因此缺陷可能一直未暴露；本次重新触发授权的具体原因未确认。
- 新增共用隐私弹窗，挂载于学籍/段位认证、队伍和个人资料页面；加载真实指引名称，提供官方指引入口。原生同意事件回传匹配按钮 ID 后由微信恢复原选图/头像流程，不重复调用选图。拒绝、组件卸载或页面未就绪及时结束等待；保留并发回调，忽略关闭后的迟到响应，加载失败可重试。
- 认证截图、段位截图、队标和个人头像增加错误反馈与空结果保护；主动取消选图不报故障，权限失败提示检查相册/相机设置。补齐旧测试和既有离线预览工具对新增依赖的适配。
- 验证：272 项 Node 测试全部通过，含 16 项新增隐私与上传回归；21 份 WXML、23 份 WXSS 微信原生编译通过。375/320 宽本地虚构预览检查正常、长指引名称、加载和失败状态，无横向溢出。测试与预览不调用真实微信授权、不选择或上传真人图片，不替代真机验收。
- 发布：必须更新小程序前端，由维护者上传、审核和发布；后端与数据库无需部署或迁移。体验版需分别检查首次同意、拒绝后重试、指引打开和四类图片入口。本次未上传/发布小程序，未提交或推送 Git；临时预览仅在内存运行，不生成入库文件。

### 2026-09-14 QQ 群自荐与招募协作表

- 在腾讯文档创建[2026届起源杯｜个人自荐与队伍招募（QQ必填）](https://docs.qq.com/sheet/DQm94ZWJjTHpITVVF)，分为「个人自荐」「队伍招募」两个工作表，分别提供身份、段位、时间、自荐内容或队伍招募要求等字段。两区 QQ 联系栏高亮并标注必填，约定每人/每队一行、仅修改自己的信息，完成组队后更新状态。
- 已固定前三行和首列，分享权限核验为「所有人可编辑」，可将链接发到 QQ 群共同填写。当前为共享空表及填写说明，QQ 必填属于填写要求，不是收集表的提交校验；未导入用户资料或实际 QQ。云端创建未生成本地临时文件。

### 2026-09-13 三类微信订阅通知（后端已部署）

- 维护者确认邀请、申请与补 QQ 三种都需要，并明确允许修改后端。沿用已读取并核对字段的「队伍通知」模板，线上 `.env` 配置三类事件；小程序已有组件按模板 ID 去重并动态列出用途，不新增前端代码或数据库迁移。
- 新增真实 `captain_qq_reminder` 事件：仅响应本次接受该模板的订阅，为当前队长尚未填写 QQ 的可见队伍生成提醒，不扫描旧订阅自动催填。授权与提醒同事务保存；按队伍 ID 和事件类型唯一约束，每队最多一次，重复授权不重复创建，失败或结果未知也不自动重发。通知短语为「补充QQ」，点击进入「我的队伍」。
- 发送前重新检查队长归属、QQ 是否已补填、队伍/用户可见性、真实微信标识与模板授权；失效则跳过。邀请和申请保留各自真实业务来源，不借用其记录发送资料提醒。
- 验证：新增 15 项补 QQ 回归，两个微信通知模块共 38 项通过；本地与实际发布镜像全部 271 项后端测试通过。所有微信网络调用均模拟，覆盖事务回滚、跨会话幂等、旧授权、资料/身份变化、路由与超时不重发；不代表真实 MySQL 并发或真机送达验收。
- 部署 `wechat-qq-20260913-193458`，仅更新服务源码 `wechat_service.py` 和微信配置。实际运行镜像及源码摘要匹配；公网配置接口返回三种事件与同一模板，队伍详情为 200。接收者仍需主动订阅，不能以配置成功当作消息已送达。
- 原配置、原源码、旧镜像和验证结果保留于 `/home/ubuntu/releases/wechat-qq-20260913-193458`，本次上传包及构建/测试 staging 已清理。仅维护本日志、部署与测试说明，未上传或发布小程序，未生成临时总结文件。

### 2026-09-13 队伍名称调整（线上数据）

- 按维护者指令，将唯一以「男同学弟」开头的队伍 ID 35 改名为「六道口泥头车青训」。事务内核对原名、目标名称唯一性和更新行数，仅修改队名，其他队伍字段保持不变；公网详情已确认新名称生效。无源码或部署变更，未生成临时文件。

### 2026-09-12 队伍报名空昵称导致 500 修复（后端已部署）

- 线上确认队伍 34 报名赛事 8 多次返回 500：成员 113 未完成学籍认证，昵称与游戏 ID 同为空；原提示列表加入 `None`，随后中文分隔拼接触发 `TypeError`。报名记录仍为 0，故障与 QQ 字段和迁移无关。
- 学籍与段位资格提示统一按去除空白后的昵称、游戏 ID、用户 ID 回退，保证提示始终为文字且不暴露学号。未认证者仍不得报名，昵称为空本身不成为报名限制，历史队伍缺 QQ 也不会被整队报名额外拦截。
- 在已有 `test_registration_routes.py` 增加 5 项真实 ASGI／隔离数据库测试，覆盖 null/空串/空白名称、两类认证、正常报名、单条/批量审批、无报名写入与人工认证提示隐私。报名专项 26 项通过，实际发布镜像全部 256 项后端测试通过。
- 部署 `registration-label-20260912-215130`，只更新 `match_service.py`，无数据库迁移和前端改动。用真实 MySQL 队伍资料运行同一报名路由并禁止所有写入，验证返回 400「以下队员未完成学籍认证，无法报名：用户113」，报名数不变；公网赛事、队伍、招募筛选均为 200，QQ 登录可见规则保持。
- 旧镜像、原文件、测试与验证记录保留于 `/home/ubuntu/releases/registration-label-20260912-215130`；本次上传包与构建/测试 staging 清理。没有代替队伍报名、认证或更换成员，也未发送 QQ 补填提醒。

### 2026-09-12 队长 QQ 后端与数据库部署（前端由维护者发布）

- 维护者确认仅由代理处理后端和数据库，前端自行发布，并已告知旧客户端不传 QQ 无法建队。部署代码为 `bed8d27`，线上源码与镜像核对仅 5 个 QQ 文件有差异；从实际旧镜像构建新镜像，未修改其他业务或服务器环境配置。
- 发布版本 `captain-qq-20260912-144117`：实际待发布镜像 251 项隔离测试通过，再停止 API 写入、备份数据库，执行幂等 `team_captain_qq` 迁移并切换后端，刷新 nginx 上游。首次迁移新增列，第二次无改动；迁移前后 16 张表全部旧列的数据摘要与行数一致。
- 验证：真实 MySQL 字段为可空 `VARCHAR(12)`，8 支历史队伍 QQ 仍为空；登录详情可读、游客（含空 Authorization）隐藏、队伍列表不批量返回 QQ。公网 OpenAPI 已要求 `captain_qq` 并提供联系更新路由，赛事、队伍、招募筛选接口均为 200；未向真实队伍写入示例 QQ，未创建测试队伍或改变队长归属。
- 回滚数据库、旧源文件、旧镜像与验证记录保留在 `/home/ubuntu/releases/captain-qq-20260912-144117`。回滚旧后端可保留新增列，不删除已补录 QQ；本次上传、构建与测试 staging 已清理。未上传或发布小程序；未给缺失 QQ 的队长发消息，等待维护者明确下令。

### 2026-09-12 建队强制填写队长 QQ（待配套发布）

- 新建队伍增加队长本人常用 QQ 必填项，前后端校验 5–12 位 ASCII 数字、首位非零并去除首尾空格，以字符串保存；缺失、非法格式不能创建队伍或使旧申请失效。仅校验格式，不代表已验证号码归属。
- 新增 `teams.captain_qq` 及幂等 `team_captain_qq` 迁移，历史队伍保持空值。当前队长可在「我的队伍」补填/修改，队员、其他用户、非队长管理员不可修改；更新加锁且失败回滚，不改变阵容、评分、报名和申请记录。
- 按维护者确认，仅登录用户在队伍详情查看 QQ，游客隐藏，公开队伍列表不增加 QQ。我的队伍、所有队伍详情和两个管理队伍入口显示联系方式及复制；历史未填写明确提示。兼容小程序游客请求的空 Authorization 头，非空非法令牌仍拒绝，学号隐私规则不变。
- 表单失败保留输入并防连点；关闭、切换队伍/账号和卸载后忽略迟到响应，保存前启动的刷新不会还原旧 QQ。建队输入框明确占满可用宽度，预览只在实际使用组件时加载组件样式，避免预览中相互污染。
- 验证：251 项 Python、256 项 Node 测试通过，新增 14 项后端和 19 项前端 QQ 回归；20 份 WXML、22 份 WXSS 微信原生编译通过。375/320 宽虚构资料预览覆盖建队、历史补录、登录/游客详情及管理入口，无横向溢出。测试未访问线上数据库或真实剪贴板，不代替真机验收。
- 发布要求已更新 `deploy.md`：新小程序审核可发布后安排迁移与前后端切换，不能提前单独部署必填后端；旧版不传 QQ 将被拒绝。此次未部署后端、未上传或发布小程序；仅保留源码、测试、可复用预览与本日志，临时截图用后清理。

### 2026-09-12 招募大厅段位与身份筛选（后端已部署）

- 全部选手新增段位、身份两个筛选器，可与昵称/游戏 ID 搜索组合；清除筛选保留已提交关键词。段位含 D 至 A++ 各档、普通 S（旧 S / 0–9 星）、金 S（10–24 星）、钻 S（25–49 星）、魔王 S（50 星）及待认证；身份为新生、老生、待确认。已入队、未报名用户继续展示，隐藏用户继续排除。
- 筛选在数据库分页之前执行，后续游标页携带相同条件，不在已加载列表上做局部筛选。身份沿用人工优先、九位 ASCII 数字学号按当前年份判断的既有规则；未认证、非标准且未人工确认者归待确认。段位与学籍认证独立，不改身份或评分数据。
- 前端独立刷新选手列表，不重拉队伍公告或丢弃招募编辑草稿；切换条件即清除旧游标，忽略旧筛选及旧分页的迟到响应。加载失败可保留条件重试。后端回显 `filters`，旧服务器若忽略新参数，新前端明确报错，避免把全量结果当成筛选结果。
- 验证：237 项 Python、237 项 Node 测试通过；分别新增 12 项服务端与前端测试，覆盖段位边界、身份人工覆盖/跨年/非标准学号、组合搜索和分页、隐私字段、非法参数、加号编码、异步竞态、重试及草稿保留。20 份 WXML、22 份 WXSS 原生编译通过，375/320 宽虚构数据预览无横向溢出。
- 已部署 `recruit-filters-20260912-133023`，仅更新两个后端文件，无数据库迁移或业务数据修改。实际发布镜像 237 项测试通过；真实 MySQL 中 64 种段位/身份组合与 Python 规则只读核对一致，公网筛选回显和非法参数 422 验证通过，旧客户端无筛选请求兼容。
- 回滚源文件、镜像和验证记录保留于 `/home/ubuntu/releases/recruit-filters-20260912-133023`；本次服务器构建/测试与上传包、本地预览截图及新增测试缓存已清理。代码同步远程，筛选界面仍需上传发布新版小程序，本次未上传或发布小程序。

### 2026-09-12 管理后台队伍详情补全（前端）

- 管理后台原队伍弹层仅显示昵称、段位和评分，且误读详情接口未返回的队长名、队伍总分。新增复用 `team-profile` 组件，同时用于「队伍管理」与「赛事管理」中的队伍卡片；从本次成员列表计算最高五人总评分、队长昵称及实际人数。
- 详情展示队标、简介、审核状态、创建日期、当前阵容，成员包含头像、昵称、游戏 ID、用户 ID、队长标识、完整段位徽章/称谓、水平分、学籍认证和新生/老生/身份待确认。汇总新老生、待确认及已认证人数；未认证或未知身份不算作老生。仅使用展示字段白名单，不复制学号、认证凭证或微信标识。
- 赛事队伍弹层增加「队伍资料」「本场战绩」两个页签，保留分阶段胜负、BO3 地图比分和导出；切换到页签顶部，不因切换重复请求队伍资料。两个入口即时显示加载状态，失败可重试，切换队伍或关闭后不接受旧响应。
- 验证：225 项 Node 测试通过，新增 12 项涵盖真实接口字段缺失、身份/段位、隐私字段、空数据、重试及异步竞态。20 份 WXML、22 份 WXSS 使用微信原生编译器通过；375/320 宽离线预览检查队伍资料与阵容，无横向溢出。离线预览仅使用虚构资料，不能替代真机验收。
- 沿用现有 `getTeam` 接口及管理员鉴权，无后端或数据库变更。需要上传并发布新版小程序才能在线显示，本次未上传或发布小程序。临时预览与截图用后清理，长期结论仍记录于本日志。

### 2026-09-12 入队申请失效修复与代码归档

- 根因：用户加入或创建队伍后，其他队伍的待处理申请、邀请没有关闭，队伍申请列表、消息页和主页数量仅按 pending 统计，已入队用户仍显示为可审核。原入队校验会阻止重复入队，但遗留待办误导队长。
- 增加独立的 `invalidated_at` 标记，保留历史状态，不把系统失效伪装成队长拒绝。通过申请、接受邀请、直接入队和创建队伍时，在同一事务内使其余待处理请求失效；成功来源保持通过。退队、踢人、解散及管理员删队后不恢复旧请求，需要重新提交。
- 队伍列表、消息列表、主页计数和微信出站发送统一过滤已失效或已入队的申请人。处理入口按队伍、用户、来源请求顺序加锁；成员、评分、报名与失效标记共同提交或回滚。没有代替用户同意申请、邀请或改变其队伍归属。
- `team_request_invalidation` 迁移可重复执行，给旧表添加可空列并清理当前已入队用户的遗留 pending。部署前停止旧 API 写入、备份数据库与镜像，校验迁移只改变预期失效标记，其他全表数据指纹保持一致。
- 验证：225 项后端测试（含 15 项申请生命周期专项）、213 项 Node 测试通过；实际发布镜像的 225 项后端测试也通过。专项覆盖各入队/移除路径、重新申请、完整事务回滚、其他用户与已处理记录保护、消息过滤、迁移幂等；微信请求均为模拟。
- 已部署 `team-requests-20260912-125211`：3 条历史申请失效，无待失效邀请；公网登录态队伍申请、消息列表与主页计数一致，公开招募接口正常。查询期间另有一条申请已正常通过，最新有效申请为 1 条；用户 114 的原申请仍待处理且未入队。此次后端修复无需重新发布小程序，刷新即可生效。
- 回滚数据库、源文件、镜像与验证记录保留在 `/home/ubuntu/releases/team-requests-20260912-125211`，本次服务器构建/测试目录和新增模块的本地临时缓存已清理。按维护者要求归档此前累积的前后端、迁移及测试代码；宣传 PDF 留在本地并精确排除提交，不删除用户素材，不新增任务总结文件。招募等既有前端改动仍需单独上传发布，微信真实模板仍待配置。

### 2026-09-12 全部选手、新老生标识与历史赛事入队拦截修复

- 招募大厅默认展示全部非隐藏用户，不按是否报名、是否已有队伍或是否认证筛除；提供昵称/游戏 ID 搜索、游标分页、队伍状态及队长邀请入口。队伍招募作为另一页签保留。队伍页“全部选手”直接进入新大厅。
- 选手列表、入队申请（消息页/队伍页）和入队后的阵容资料显示新生、老生或身份待确认；统一沿用认证状态与有效身份计算，管理员人工身份优先。非标准学号、未认证者不猜作老生；公开接口继续不返回学号，也不以学号进行模糊搜索。
- 线上只读核对用户 114 加入队伍 30「银河战舰Vitality」被阻止：用户已认证、段位 B+、无队伍，目标队伍仅 3 人；唯一关联赛事 ID 7 已结束，但仍有 seed=3、上区、playoff 进度。新增入队预检查误将历史编排当作当前锁定，与原来的补员规则不一致。
- 修复：已结束赛事不再参与入队认证要求、队伍人数上限或约束检查，实际入队也不为已结束赛事补报名，历史参赛名单保持原样。尚未结束赛事继续校验认证和人数；锁定后禁止新增参赛队伍，既有参赛队伍沿用此前允许补员的规则。未替用户 114 执行申请、邀请或入队。
- 验证：210 项后端测试、213 项 Node 测试通过；WXML/WXSS 原生编译通过，375/320 宽离线预览无横向溢出。新增用例覆盖全部用户展示、身份计算、学号隐私、申请与入队后身份一致，以及历史赛事/当前赛事混合时的补员与名单保护。
- 已部署 `recruit-directory-20260912-122802`，仅更新 5 个后端文件，无数据库迁移；真实部署镜像的 210 项测试通过。线上用户 114 / 队伍 30 的只读入队条件检查通过，用户仍未入队；公开选手目录和队伍身份字段验证通过。回滚备份保留在 `/home/ubuntu/releases/recruit-directory-20260912-122802`，服务器本次 staging/tests 已清理。小程序修改待上传发布，未提交或推送 Git。

### 2026-09-12 招募大厅、入队资料与消息通知（后端已部署，小程序待发布）

- 查明“约赛通知后端未更新”的原因为之前采用定向补丁部署，未包含已完成的约赛消息功能。先部署 `schedule-20260912-113916`：真实镜像 55 项测试通过，登录态消息接口返回 200，原 404 已消除。
- 新增招募大厅：队伍审核通过后队长即可发布、修改、关闭招募，不要求事先报名赛事；每队保留一条公告，公开页分页展示非隐藏、已审核队伍。主页、侧边导航与我的队伍均有入口；原人才市场继续展示已报名自由人。
- 招募大厅、全部队伍及原队伍申请入口统一支持选填 200 字入队自我介绍。队长在队伍页与消息页可查看头像、昵称、游戏 ID、段位、水平分、认证状态、新老生身份、个人介绍及本次自我介绍，再同意/拒绝。未认证者不显示已确认的新老生身份。
- 申请资料使用明确字段白名单，不返回学号、AI 候选学号、认证截图、段位截图或微信 OpenID；核对公开队伍、人才市场、赛事、名人堂响应以及本人/管理员接口鉴权。本人及授权管理审核按既有权限查看，按学号精确查找保留。
- 按维护者追加要求，赛事主页显示当前账号的待处理邀请、队长待处理申请和未读约赛通知数量，各分类直接进入消息页对应位置；无待办时隐藏，失败提示重试，退出或换账号不沿用旧数量。消息页增加申请资料和审核按钮。
- 入队申请和邀请复用事务锁，拦截重复待处理、隐藏账号/队伍、被驳回队伍、已报名满员及已分组锁定的赛事。申请/邀请和微信出站记录一并提交；微信网络请求不在入队事务内执行。
- 微信一次性订阅支持 `team_invitation` 与 `team_application` 两类事件、模板字段映射、用户主动点击授权、服务端令牌缓存与持久化出站队列。没有模板或授权时仍保留站内流程；微信最终校验可用订阅次数；超时/中断不盲目重发，明确令牌失效仅重试一次。微信模板未选用，线上保持 `WX_SUBSCRIBE_ENABLED=false`，本次未向真人发送测试推送。
- 部署 `recruitment-20260912-120827`：基于当时线上镜像只替换本次 12 个应用文件，添加 compose 配置和 `recruitment_notifications` 新表迁移；本地与实际部署镜像各 203 项后端测试通过，211 项 Node 测试通过，19 份 WXML、20 份 WXSS 原生编译通过。375/320 宽离线检查无横向溢出。公网招募/订阅配置、需登录的主页汇总/申请/约赛/我的招募接口验证通过。
- 回滚源文件与镜像记录保留在 `/home/ubuntu/releases/recruitment-20260912-120827`（镜像 `bjfu-backend:rollback-recruitment-20260912-120827`）。本次服务器 staging/tests 目录及本地临时预览已清理。未提交/推送 Git，未上传/发布小程序。
- 待办：上传审核新版小程序；按 `deploy.md` 选择并配置真实订阅模板，使用自愿测试账号验证微信授权、送达和跳转。离线预览与模拟微信请求不代替真机验收。

### 2026-09-12 学号位数与新老生判定（已部署）

- 按维护者确认，仅对 9 位数字学号按前两位入学年份自动判定新老生；其他格式返回身份未确定，不自动归为老生。管理员已明确设置的身份继续优先生效，用于硕博等人工认证。
- 非 9 位数字候选学号保留给管理员核验，不由 AI 自动通过或仅因格式直接驳回；审核说明提示联系管理员 QQ 3761215994。个人报名及整队资格不足时，后端沿用已有错误提示通道告知需要人工认证；未确认的身份不计入三名新生要求。
- 仅修改后端及已有回归测试，未修改前端、未新增数据库字段。187 项后端测试通过，新增 5 项测试覆盖 9 位前置判断、非 9 位人工复核、管理员身份设置和整队报名资格。
- 经维护者要求部署版本 `identity-20260912-005553`：基于当时线上镜像定向更新三个服务文件，实际发布镜像的 58 项认证、报名回归测试通过；公网接口、登录态接口和运行容器内 5 组学号样例验证通过，人工身份设置保持优先。没有执行线上数据修正、提交或推送 Git。
- 回滚材料保存在 `/home/ubuntu/releases/identity-20260912-005553`，原镜像标记为 `bjfu-backend:rollback-identity-20260912-005553`；本次临时构建、测试目录已删除，全程未生成本地临时脚本或部署包。

### 2026-09-11 起源杯规则宣传图

- 依维护者提供的墨绿、薄荷绿、白色与金色配色制作三组 16 队赛制规则长图，包含新生资格、各阶段晋级、六强 BO3、同分排序及 1000 元奖金分档；校正直升种子与交叉半决赛的连线表达。
- 成图保存在被 Git 忽略的 `artifacts/promotion/起源杯-三组赛事规则-20260911-v1.png`，不放入小程序包，不提交宣传产物；未修改比赛配置或线上数据。
- 9 月 12 日按要求导出同名单页 PDF，原比例无损嵌入 887×1774 像素图片；提取 PDF 内图像后逐字节校验一致，并经渲染检查无裁切。PDF 保存在同一被忽略目录，临时核验图片已删除；这是保留原图质量的转换，不增加源图细节。

### 2026-09-11 删除脚本 BOT 账号（已执行）

- 按维护者追加要求，物理删除批次 `BOT_MATCH_7_FILL_20260911` 的用户 ID 27–91，共 65 个账号；核验批次标记、脚本 OpenID、普通用户角色及隐藏状态后执行，未按昵称猜测范围。
- 执行时 BOT 队伍、成员、报名、申请和邀请关联已经不存在。数据库事务中逐表核对，除这 65 个用户外没有修改其他数据，剩余 29 个账号。操作前赛事 ID 7 已无对局，本次未删除队伍、赛事、对局或冠军档案。
- 操作前数据库备份及结果保存在服务器 `/home/ubuntu/releases/delete-bot-users-20260911-231333`。此次为数据操作，无代码部署、无 Git 提交，脚本通过标准输入执行，没有生成本地临时文件。

### 2026-09-11 测试数据隐藏与赛事清理（已在线生效）

- 维护者明确保留 ID 7「起源杯测试赛事」，删除 ID 1、2、3、4、6；只隐藏脚本创建的 BOT 账号、队伍，后台仍可回溯。删除操作在同一数据库事务中完成，并逐表比对实际变更与预期变更。
- 精确核验批次 `BOT_MATCH_7_FILL_20260911`、BOT 微信标识与成员构成：隐藏用户 ID 27–91 共 65 个、现存队伍 ID 16–27 共 12 支。原第 13 支 BOT 队伍此前已删除；没有根据昵称关键词或参赛身份隐藏真人数据。
- `users`、`teams` 新增 `is_hidden`，默认不隐藏，迁移可重跑且不自动识别或处理数据。公开队伍列表、详情、选手段位榜、人才市场和冠军名册过滤隐藏记录；榜单先过滤再排名、分页。管理员仍可查看完整队伍资料和赛事记录。
- 保留赛事的公开队伍页不再列 BOT 卡片，历史对阵对手显示「已隐藏测试队伍」。保留原队伍 ID、胜者、比分和所有 38 场对局，避免把隐藏对手误判成轮空；后台仍显示原队名。赛事冠军记录保持原样。
- 验证：182 项后端测试通过，其中 7 项隐藏专项测试也在实际发布镜像的无网络隔离环境中通过；线上公开与管理接口核验通过。真人账户、队伍及保留赛事的数据逐表校验无非预期变化。
- 服务器恢复材料：`/home/ubuntu/releases/visibility-20260911-1555`，包括操作前数据库备份、后端源码备份、补丁及操作结果；回滚镜像 `bjfu-backend:rollback-visibility-20260911-1555`。本次只部署隐藏补丁，约赛通知及前端排名调整仍待单独发布。
- 已删除本次本地临时连接脚本、清理脚本、核验前副本、补丁和缓存，以及服务器临时构建、测试目录；仅保留必要源码、迁移、回归测试与服务器恢复材料，没有新增独立总结文档。

### 2026-09-11 队伍列表与分阶段战绩（本地完成，待发布）

- 用户端与管理端的队伍页统一为一份赛事列表，移除重复的阶段队伍滑动区；外层只显示阶段、名次与队名下的实际组别。阶段对阵页签、管理端报名审批、分组入口及队伍导出保留。
- 按维护者确认，依据最终晋级成绩与组内最终名次划分奖金档，同档并列、种子仅稳定排列，不跨组比较胜负来拆档。组内依策划案按胜场、净胜回合、队伍水平分确定名次；缺场、缺结果或三项完全同分时显示名次待核实，不以种子代替裁判裁决。
- 队伍左侧保留冠、亚、四强、六强及决赛标识；其他已出局队伍按奖金档显示区间。三组 16 队：7–8、9–10、11–13、14–16；四组 16 队：7–8、9–12、13–16；四组 20 队另有 17–20。仍在比赛的队伍只标阶段，待分组/待审核不标最终名次。队名下标注组别及已确认的组内名次。本次仅调整排名，不实现奖金计算或发放。
- 点击队伍卡片展开分阶段战绩：挑战者小组、附加赛、传奇分区、淘汰赛分别列出对手、比赛结果和胜负汇总。BO3 一轮算一场，地图小分单独列出且本队比分在前；轮空、未结束和缺失胜者的对阵单独标明，不计作胜负。
- 验证：193 项 Node 前端测试通过，含新增 8 项排名和详情回归；同步更新此前约赛消息的旧测试桩。两个页面 WXML/WXSS 原生编译通过，320px/390px 用户端、管理端列表与详情共 8 个离线布局检查通过。本次没有改动后端或线上数据，没有提交、推送或发布小程序；仍需微信真机验收。
- 奖金档调整后：200 项 Node 全量测试通过，其中 15 项排名/详情回归覆盖三种赛制规模、历史组别保留、组内第三/第四分档及同分待核实。只读核对赛事 7：M7-05 为 9–10 档，各挑战者小组第三/第四分别为 11–13、14–16 档。两端 320px/390px 共 4 项区间文字布局检查通过，本轮未生成预览文件。

### 2026-09-11 仓库整理

- 14 份独立更新记录的长期结论归入本日志，远程移除其中已跟踪的 8 份。核验后按维护者要求删除仓库外临时备份与清理脚本；已提交过的旧文件仍可从 Git 历史找回。README 与测试说明不再重复堆叠阶段汇报。
- 移出 3 个旧部署压缩包、两次发布的说明/校验清单、写死旧版本及“无迁移”结论的 `prepare_release.py`，以及只修改固定赛事 ID 的一次性 `fix_matches.sql`。常规部署使用部署指南，旧脚本不再作为当前发布入口。
- 微信开发者工具私有配置停止跟踪但保留本机文件；发布目录、宣传生成目录及临时输出目录加入忽略规则。宣传素材、策划案、源码、测试、数据库迁移和可复用预览工具保留。
- 清理通过普通提交同步远程，不重写历史；历史提交仍能查看旧文件。
- 整理验证：工作区与暂存区文档链接有效，220 个未编辑文件和 24 个移出文件的 SHA-256 与整理前一致，暂存区无业务/测试代码；本次未改运行逻辑，不重跑业务测试。

### 2026-09-11 约赛消息（本地完成，待发布）

- 提交/修改时间通知另一方队长；同意、拒绝、撤回通知提议方或另一方；管理员修改窗口导致约定作废时通知双方，已开赛/已结束对阵不清空时间。
- 约定和通知同事务保存；重复提交不重复通知。收件箱按用户隔离，支持游标分页、未读计数、幂等已读；保留事件时的队伍与时间快照。
- 消息页点击通知后进入对应赛事和场次，重新检查最新状态与当前队长权限；查看通知不自动确认约赛。进入页面/下拉刷新读取，不含微信订阅推送、后台轮询或 WebSocket。
- 新增 `schedule_notifications` 表与 GET `/api/notifications/schedules`、PUT `/api/notifications/schedules/{notice_id}/read`；旧记录不补造通知。
- 历史验证：相关后端 45 项、前端 34 项通过；两个页面 WXML/WXSS 原生编译通过，320px/390px 离线预览无横向溢出。

### 2026-09-11 BO3、认证与管理待办

- BO3 1/4 决赛报 500 的根因是线上冠军表缺少 `manual_created_by` 等补录字段；录分同步冠军档案时整体回滚。迁移后恢复，代码对冠军表缺表/缺列返回明确 503，保留回滚而不跳过冠军同步。
- 学信网截图允许身份证号和人像打码；可见学号作为 `users.ai_student_id` 候选保留。人工审核预填候选，核对后学号与认证一起保存；历史缺学号账号可重新识别并确认。不得猜测被遮挡信息，人工修正与更新后的凭证不被迟到结果覆盖。
- 管理表单扩大输入区、字号和日期控件，增强对比度；待办显示具体类别、数量和赛事入口。人工驳回的旧凭证、已锁定赛事的历史待审报名不再制造不可处理的待办；失败明确提示，避免把旧数据当新待办。
- 后端已随 `20260911-140444` 部署，小程序界面待发布。该次迁移前后 12 张表原有数据摘要一致，运行容器 34 个文件匹配发布清单，赛事与名人堂只读接口返回 200，未代填比分。
- 回滚资料保存在服务器私有目录 `/home/ubuntu/releases/20260911-140444-18UTUt`，镜像标签 `bjfu-backend:rollback-20260911-140444`。应用回滚不自动恢复旧数据库覆盖后续数据。
- 历史验证：认证修改时 Python 全量 155 项、Node 全量 169 项通过；后续打码场景相关 Python 39 项、Node 27 项通过。BO3 相关 Python 56 项、Node 3 项通过。以上为不同阶段结果，不相加为当前全量测试数。

### 2026-09-11 测试赛事数据

- 曾按要求为赛事 7 添加 13 支 BOT 队伍、65 个测试账号，将报名填至 16 队；操作时赛事报名保留待审核，没有代做种子、分组或赛程。
- 数据识别标记 `BOT_MATCH_7_FILL_20260911`，队名 `BOT-M7-01` 至 `BOT-M7-13`，队伍 ID 16–28、用户 ID 27–91；只用于追踪，不作为自动删除条件。赛事之后已推进，不能把填充时状态当作当前状态。
- M7-05 小组赛 2 胜 1 负、附加赛 1 胜 1 负，合计 3 胜 2 负。附加赛只有冠军晋级，M7-06 为 2 胜 0 负，因此 M7-05 第二出局符合三组赛制。

### 2026-09-08 名人堂、导航与报名锁定

- 名人堂分赛事冠军与认证段位榜。自动冠军必须来自完整有效六强淘汰赛决赛链，随比分原子保存历史阵容快照；同段同星并列，未认证申请不参加排名。
- 管理员在“赛事管理 → 已结束赛事 → 补录冠军”录入历史队名、日期及阵容，亚军/比分可留空，未知信息不伪造。公开页面标注管理员补录；已有正式冠军通过决赛更正，完整旧决赛可显式调用 `backfill_champions` 归档。
- 补录使用版本号防覆盖，操作人/备注仅管理端可见；已有数据库必须执行 `manual_champions` 迁移，`create_all()` 不会给旧表补列。
- 管理后台增加四类待办入口与按赛事报名计数；摘要固定两条聚合查询，前端按账号缓存 30 秒、复用在途请求，审核后失效。
- 名人堂融入首页快捷入口，非首页增加明确的“返回赛事”，复用页面栈并提供返回失败兜底。
- 通过报名但未编排显示“待分组”；种子、分组、对阵或后续阶段记录使参赛名单锁定，新增报名/审批不能绕过。种子与分组同事务保存，失败不留下半份编排。
- 历史验证：管理工作台与补录版本 Python 124 项、Node 153 项通过；17 个 WXML、18 个 WXSS 原生编译通过。

### 2026-09-07 前端、时间与三/四组赛制

- 统一白、深蓝、青绿色界面；补齐 D、C/B/A 基础及加号段位、S/金 S/钻 S/魔王 S 等 15 个本地图标，首页个人卡片与认证、资料页共用映射。
- WXML 条件分支与循环拆开，修复人才市场 `wx:else` 编译失败；模板结构检查纳入回归。
- 报名起止时间单独设置、可清除、按北京时间解释；排期窗口按实际组数和阶段命名，支持自定义组名。报名时间与比赛约赛窗口用途不同。
- 三组：4 支直升 + 3 个小组冠军 + 1 个附加赛冠军；四组：4 支直升 + 4 个小组冠军，不生成或显示附加赛。前序比赛/晋级未完成，不能提前分传奇组。
- “通过报名”Not Found 曾由线上缺少新审批接口引起；缺失路由需更新后端，不用旧接口绕过规则。
- 历史验证：Python 68 项、Node 63 项通过，14 个 WXML 原生编译通过，含三组和四组完整流程。

### 2026-09-06 业务一致性修复

- 修复重复学号认证、个人报名升级整队报名、旧邀请重放、新生资格变动、并发入队超员；成员、报名和评分采用行锁与完整事务。
- 小组/附加赛须完整结束才能晋级；对阵生成后不能重分组，比分已用于后续晋级后不能直接改写。排名保留对未晋级队伍的该阶段成绩。
- AI 失败与首次段位转人工，重传凭证清除旧审核状态，迟到结果不覆盖新凭证。历史错误数据不自动重写。
- 历史验证：Python 39 项、Node 6 项通过，含完整赛制及双进程入队竞争。

## 历史技术复盘

以下为早期开发经验，具体部署步骤以维护中的部署指南为准。

## 一、项目概览

北京林业大学 CS2 比赛报名小程序，全栈项目，包含：

| 模块 | 技术 |
|------|------|
| 后端 | FastAPI + SQLAlchemy 2.0 + Pydantic v2 |
| 数据库 | MySQL 8（从 SQLite 迁移）|
| 认证 | 微信真实登录（jscode2session）+ JWT |
| AI 审核 | 智谱 GLM-5V-Turbo（多模态视觉）自动审核学信网截图 |
| 前端 | 微信小程序原生（WXML/WXSS/JS）|
| 部署 | Docker + docker-compose + nginx + HTTPS（Let's Encrypt）|
| 服务器 | 腾讯云轻量应用服务器（Ubuntu 22.04）|

---

## 二、踩坑记录（按阶段）

### 1. 后端开发阶段
- **路由注册遗漏**：`team.py`、`auth.py` 忘记在 `main.py` 中 `include_router`，导致接口 404
- **路由顺序**：`/talent-market` 等固定路径路由必须放在 `/{team_id}` 动态路由**前面**，否则被动态路由拦截报 422
- **拼写错误**：`Depens(require_admin)` → `Depends`，运行时才暴露
- **service 层返回 None 未处理**：路由层要检查 service 返回 None（404），否则永远返回成功
- **ValueError 未捕获**：service 抛 ValueError 会导致 500，路由层要 try/except 转 HTTPException
- **IntegrityError 未捕获**：唯一约束冲突（如队名重复）会导致 500，要 try/except/rollback

### 2. 数据库迁移（SQLite → MySQL）
- **SQLite 不支持字段注释**：MySQL 支持 `comment="..."`，Navicat 可见，所以换 MySQL
- **pymysql 连接**：`mysql+pymysql://user:pass@host:3306/db?charset=utf8mb4`
- **本地 MySQL 免安装版**：重启后要手动 `start_mysql.ps1` 启动

### 3. AI 截图审核
- **DeepSeek 纯文本不支持看图**：必须选多模态视觉模型（智谱 GLM-5V-Turbo、通义 Qwen-VL）
- **图片编码**：图片转 base64 传给视觉模型
- **置信度分级设计**：≥0.9 自动通过/驳回，0.6~0.9 转人工，<0.6 必须人工——AI 永远不能 100% 拍板
- **AI 失败降级**：AI 调用异常不能影响上传主流程，要 try/except 转人工审核

### 4. 前端小程序
- **WXSS 空 `{}` 报错**：空文件留白即可，不能写空对象
- **`es6: false`**：关闭 Babel 转译，基础库原生支持 async/await，避免 regeneratorRuntime 报错
- **标签必须匹配**：`<view>` 必须 `</view>` 结尾，混用导致整页不渲染
- **`wx.switchTab` vs `wx.navigateTo`**：tabBar 页面必须用 switchTab
- **`wx.chooseMedia` 返回 tempFilePath**：不是 filePath，容易取错字段
- **按钮默认样式**：纯展示按钮用 `<view>` 代替 `<button>`

### 5. 部署（Docker + 服务器）—— 最惨烈阶段
- **Docker Hub 拉镜像超时**（国内网络）：配置镜像加速器 `/etc/docker/daemon.json`
  ```json
  {"registry-mirrors":["https://mirror.ccs.tencentyun.com","https://docker.m.daocloud.io"]}
  ```
- **pip 装依赖超时**：`sqlalchemy==2.0.35` 找不到 → Dockerfile 加国内 PyPI 镜像
  ```dockerfile
  RUN pip install -i https://mirrors.cloud.tencent.com/pypi/simple -r requirements.txt
  ```
- **Dockerfile 不 COPY .env**：容器内读不到 .env，AI 配置、ADMIN_USERNAME 等必须在 docker-compose.yml 的 `environment` 显式注入
- **容器内用服务名通信**：`DATABASE_URL` 里 host 写 `mysql`（服务名），不是 `127.0.0.1`
- **expose vs ports**：`expose` 只在内部网络开放，不映射宿主机；对外访问必须 `ports`
- **nginx 因证书不存在反复崩溃**：443 配置引用未申请证书 → 先临时纯 HTTP，证书好后再恢复
- **数据卷 volumes**：`mysql_data`、`uploads_data` 挂载到宿主机，容器重建数据不丢
- **sudo 权限**：`usermod -aG docker ubuntu` 加入 docker 组，否则 docker 命令报 permission denied
- **tar 打包会带上 .env**：打包用 `--exclude='backend/.env'` 排除敏感文件
- **.dockerignore**：排除 .env/uploads/测试文件，防止进镜像

### 6. 域名 + 备案 + HTTPS
- **域名 vs 服务器**：域名是"名字"，服务器是"房子"，靠 DNS（A 记录）绑定
- **ICP 备案**（工信部）：域名解析到国内服务器必须备案，否则被 DNSPod 拦截（返回 webblock 拦截页）
- **备案有延迟**：备案通过后，DNSPod 拦截解除需几小时到一天
- **Let's Encrypt 申请失败排查**：报错里的 `webblock.html` 说明域名被备案拦截；报错 IP 不是服务器 IP 说明解析被劫持
- **certbot standalone 模式**：需要 80 端口空闲（先停 nginx），验证域名归属
- **证书自动续期**：cron 每天跑 `certbot renew --webroot`（webroot 模式不用停 nginx）
- **公安联网备案**：ICP 备案之后的另一个备案，网站开通 30 日内做

### 7. 微信真实登录
- **AppID 前后端都要有，但作用不同**：
  - 前端 `project.config.json`：开发者工具识别"代码属于哪个小程序"
  - 后端 `.env`：调微信接口换 openid 时的身份凭证
  - 两者必须指向同一个小程序，否则 `invalid appid`
- **AppID 是 18 位**（wx + 16 位十六进制），少一位就 `invalid appid`（errcode 40013）
- **AppSecret 只在后端**：密码必须保密，写前端会被反编译泄露
- **code 是一次性、5 分钟有效**：⚠️ 最大的坑——前端缓存 code 复用会导致 400 登录失败，每次登录必须重新 `wx.login()`
- **错误码排查**：
  - `40013 invalid appid` → AppID 错
  - `40029 invalid code` → code 过期/复用（AppID/Secret 是对的）
  - `40125 invalid appsecret` → Secret 错

---

## 三、核心知识点总结

### 后端
- FastAPI 依赖注入（`Depends`）、路由组织（routers/）、Pydantic 数据校验
- SQLAlchemy ORM：模型定义、关系、枚举、唯一约束
- JWT 认证：签发、解析、`get_current_user` / `require_admin` 依赖
- bcrypt 密码哈希
- 多模态 AI 调用：图片 base64 + Prompt + JSON 解析

### 前端
- 小程序页面生命周期、`wx.request` / `wx.uploadFile` / `wx.downloadFile`
- API 封装层（`utils/api.js`）统一 token、错误处理
- 本地存储（`wx.setStorageSync`）

### 部署运维
- Docker 核心概念：镜像（模板）vs 容器（运行实例）vs compose（编排）
- Dockerfile（镜像配方）、docker-compose.yml（服务编排）、.env（敏感配置）、nginx.conf（反向代理）
- 容器网络：服务名互访、ports 对外暴露
- 数据卷：持久化
- Linux 常用：SSH、scp、sed、crontab、systemctl

### 网络与合规
- DNS 解析（A 记录）、HTTPS/SSL 证书原理（加密 + 身份认证）
- 备案体系：ICP 备案 + 公安备案 + 小程序备案
- 微信开放平台：jscode2session 登录流程、合法域名

---

## 四、部署运维速查

```bash
# SSH 登录
ssh ubuntu@192.144.191.253

# 项目目录
cd /home/ubuntu/app

# 容器管理
docker compose ps                        # 看状态
docker compose up -d --build             # 构建并启动
docker compose logs backend --tail 50    # 看日志
docker compose restart nginx             # 重启单个

# 改配置后生效（重读 .env）
docker compose up -d

# 查数据库
docker compose exec -T mysql mysql -ucs2_user -p'密码' cs2_competition -e "SELECT * FROM users;"

# 改代码上线流程
# 1. 本地打包（排除敏感文件）
tar -czf deploy.tar.gz --exclude='backend/.env' --exclude='backend/uploads' backend docker-compose.yml nginx.conf
# 2. 上传
scp deploy.tar.gz ubuntu@192.144.191.253:/home/ubuntu/
# 3. 服务器解压 + 重建
sudo tar -xzf /home/ubuntu/deploy.tar.gz -C /home/ubuntu/app && cd /home/ubuntu/app && docker compose up -d --build
```

---

## 五、经验教训（方法论）

1. **错误要追溯到根因**：看到 `invalid appid` 不能只改配置，要理解为什么——AppID 前后端各司其职
2. **敏感信息三层防护**：不进 git、不进镜像、只在服务器本地 .env
3. **AI 永远留人工兜底**：AI 审核置信度分级，不能 100% 自动拍板
4. **一次性凭证不能缓存**：微信 code 一次性，缓存复用必踩坑
5. **国内网络是常态问题**：Docker Hub、PyPI、Let's Encrypt 都要准备国内镜像源
6. **备案是最长链路**：域名实名 → ICP 备案 → 拦截解除，每个环节都有延迟，要提前规划
